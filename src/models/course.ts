export type Course = { // comment: course entity type
  id: string; // comment: unique id for course
  code: string; // comment: course code (e.g., CS101)
  name: string; // comment: course name
  credits: number; // comment: course credits
  teacherName: string; // comment: teacher name (text for now)
  createdAt: number; // comment: creation timestamp
}; // comment: end type
