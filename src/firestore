import { initializeApp } from "firebase/app";
import { getFirestore } from "firebase/firestore";

const firebaseConfig = {
  apiKey: "DUMMY",
  authDomain: "DUMMY",
  projectId: "course-sites-1e65c",
};

const app = initializeApp(firebaseConfig);
export const db = getFirestore(app);
